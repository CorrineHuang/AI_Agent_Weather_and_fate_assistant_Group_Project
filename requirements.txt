# PyTorch (CPU version, change to GPU version if needed)
torch==2.2.0  # Install latest stable version
torchvision==0.17.0  # For vision-related models (optional)
torchaudio==2.2.0  # For audio processing (optional)

# Hugging Face Transformers & Optimization Libraries
transformers==4.36.2  # Hugging Face Transformers
accelerate==0.25.0  # Optimizes model execution
bitsandbytes==0.41.1  # Enables 4-bit quantization

# Tokenization Dependencies
sentencepiece==0.1.99  # Needed for some tokenizers
protobuf==3.20.3  # Avoids protobuf version conflicts

# Core Libraries
numpy==1.23.5  # Stable version for PyTorch compatibility
scipy==1.11.4  # Required for some transformers functions

# Optional: If using CUDA (GPU)
# Comment this out if using CPU-only
# torch==2.2.0+cu118 torchvision==0.17.0+cu118 torchaudio==2.2.0+cu118 --index-url https://download.pytorch.org/whl/cu118
